import 'package:flutter/material.dart';

class DownloaderScreen extends StatelessWidget {
  const DownloaderScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('DownloaderScreen'),
    );
  }
}
